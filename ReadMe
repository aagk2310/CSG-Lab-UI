

Run the following docker commands 

   docker-compose build
   docker-compose up

   This will build your images and start the containers. You can then access your MERN application at `http://localhost:3000` for the Node.js app and `http://localhost` for the React app.
